{
  "name": "@klaboworld/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"(scripts lint placeholder)\"",
    "export-legacy": "ts-node --esm src/export-legacy.ts",
    "github-snapshot": "ts-node --esm src/refresh-github-projects.ts",
    "new-post": "ts-node --esm src/create-post.ts",
    "infra-deploy": "ts-node --esm src/infra-deploy.ts",
    "deploy": "ts-node --esm src/deploy.ts",
    "verify-contentlayer": "ts-node --esm src/verify-contentlayer-schema.ts"
  },
  "dependencies": {
    "slugify": "^1.6.6"
  },
  "devDependencies": {
    "@klaboworld/config": "workspace:*",
    "ts-node": "^10.9.2"
  }
}
