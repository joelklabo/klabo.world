/**
 * Placeholder for the CLI that will hash admin passwords and insert rows via Prisma.
 * Real implementation arrives once Prisma client wiring + auth flows are in place.
 */
export function createAdminPlaceholder({ email }) {
    console.info(`[create-admin] would provision admin for %s`, email);
}
